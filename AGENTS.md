# AGENTS.md · foothold-go2 에서 AI 에이전트가 지켜야 할 것

> 이 파일에는 **공통 규칙만** 둡니다. 개인 작업 방식은 로컬 설정(gitignore 대상)으로
> 두십시오. 공통 파일에 개인 설정이 섞이면 다른 사람의 에이전트가 그것을 규칙으로 읽습니다.

## 0. 작업을 시작하기 전에 반드시

```bash
git pull origin main
```

옛 상태에서 갈라진 브랜치로 작업하면 AI 는 그 시점의 저장소만 봅니다.
**오래된 상태를 주면 오래된 답이 나옵니다.**

## 1. 이 저장소가 무엇인가

**공개 저장소입니다.** 코드와 재현 절차가 여기 있습니다.
문서·회의록·내부 기록은 `foothold-lab`(비공개)에 있습니다.

| 폴더 | 무엇 | 작업 영역 |
|---|---|---|
| `sim/policy/` | 학습 설정 · 보상 · 관측 · 파인튜닝 | `A/정책학습` |
| `sim/terrain/` | Isaac Lab 지형과 씬 에셋 | `A/지형씬제작` |
| `sim/eval/` | 일반화 벤치마크 · 지표 | `A/평가` |
| `sim/twin/` | 실공간 복원 · 3DGS · 렌더 · 합성데이터 | `A/트윈렌더` |
| `nav/src/` | ROS2 워크스페이스 (colcon 루트) | `B/항법` |
| `nav/maps/` | SLAM 지도 | `B/항법` |
| `nav/perception/` | LiDAR · 카메라 · 센서 융합 | `B/인지` |
| `common/` | Go2 스펙 · 좌표계 · 공통 유틸 | |

**폴더가 이슈의 작업 영역과 짝을 이룹니다.** 이슈에 `A/평가` 가 붙으면 코드는 `sim/eval/` 입니다.

## 2. 브랜치와 PR

```bash
git switch -c feature/작업명-이름     # 예: feature/terrain-gap-meang
```

- **브랜치 이름에 이슈 번호를 붙이지 않습니다.** 뒤에 자기 이름을 붙입니다.
- 이 저장소는 한 단계가 더 있습니다: `feature/…` → PR → `dev` → 검증된 것만 PR → `main`
- 추적은 커밋·PR 의 **`Closes #번호`** 가 합니다. 이것만은 반드시.
- `main` 에 직접 push 하지 않습니다.

| 이름 | 임석헌 | 맹라현 | 오현민 | 오흥재 | 이민우 |
|---|---|---|---|---|---|
| 브랜치 약자 | `lim` | `meang` | `oh` | `jay` | `lee` |

## 3. 이슈

이슈 목록에서 **템플릿을 골라** 만듭니다. 빈 이슈로 만들지 않습니다.

| 템플릿 | 언제 |
|---|---|
| 작업 등록 | 이런 작업을 하겠다고 미리 올릴 때 |
| 실험 기록 | 가설 하나, 변경 하나. 결과를 숫자로 판정할 때 |
| 막힘·오류 | 안 되는 것이 있을 때 |

**「작업 영역」과 「어느 관문까지」를 고릅니다.** 고르면 라벨과 마일스톤이 자동으로 붙고
보드에서 묶입니다. 안 고르면 팀장 텔레그램에 「분류 안 됨」이 갑니다.

## 4. 문서를 쓸 때

`guide/` 와 `report/` 로 나갈 문서는 맨 위에 다섯 줄이 필요합니다.

```
> 분류: 가이드
> 작성: 홍길동 · 2026-08-27
> 근거: 실측
> 요지: 한 줄로 이 문서가 말하는 것
> 상태: 초안
```

- **em dash 를 쓰지 마십시오.** `·` 나 쌍점을 씁니다.
- **선으로 그린 글자 도식을 쓰지 마십시오.** 표로 바꿉니다.
- 재보지 않은 것에 「확인했다」를 쓰지 않습니다. `확인됨` `추측` `미확인` `미측정` 을 답니다.

## 5. 절대 하면 안 되는 것

| 금지 | 왜 |
|---|---|
| 비밀번호 · 토큰 · 개인 연락처를 넣기 | **공개 저장소입니다.** 지워도 이력에 남습니다 |
| 조선대 강의 자료 원본을 넣기 | 조선대 저작물입니다 |
| 예산 금액을 적기 | 대외비입니다 |
| `main` 에 직접 push · force push | 남의 작업이 사라집니다 |
| 100MB 넘는 파일 | GitHub 이 거부합니다. NAS 나 클라우드에 두고 링크만 남기십시오 |
| 학습 체크포인트 · 대용량 로그 | NAS 나 클라우드에 두고 링크만 남깁니다 |

## 6. 이 환경에서 안 되는 것

- **이슈 템플릿의 `name:` 을 한 단어로 짓지 마십시오.** `작업`·`실험` 같은 한 단어 이름은
  GitHub 이 템플릿 목록에서 오류 없이 조용히 숨깁니다 (2026-08-27, 재현 6회).
  두 단어 이상으로 짓고, 바꾼 뒤에는 `issues/new/choose` 화면을 눈으로 확인하십시오.

## 7. 전체 흐름

이슈부터 머지까지의 순서와 무엇이 자동으로 도는지는
`foothold-lab` 의 `docs/WORKFLOW.md` 한 장에 있습니다.
공개본은 <https://foothold-project.vercel.app/collab> 에 있습니다.
